import { ComingSoon } from "@/components/shell/coming-soon";

export default function PluginsPage() {
  return (
    <ComingSoon
      title="Plugins"
      blurb="The plugin registry: what this hub admitted, what it refused, and the curated catalogue."
    />
  );
}
