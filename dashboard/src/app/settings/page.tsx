import { ComingSoon } from "@/components/shell/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      blurb="Hub configuration at every depth — Easy, Standard, and Advanced lenses over one configuration object."
    />
  );
}
