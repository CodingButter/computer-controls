# Review: discord: read latest direct message

Submitted by `client-7`. Signature `7ad623df893c5e28`.

## What this skill has an agent do

5 steps against discord, ending at the element the task names. Every step is a read or a navigation issued through a protocol call named below; the commons publishes no scripts and no binaries, so nothing here executes on the machine that installs it.

| step | call | why this step |
| --- | --- | --- |
| 1 | `census` | no element named: this step is a call the route makes on its own |
| 2 | `setAttention` | acts on the `window` the previous step reached |
| 3 | `describeElement` | acts on the `document text` the previous step reached |
| 4 | `describeElement` | reaches the `list` under `Private channels` |
| 5 | `describeElement` | acts on the `list item` the previous step reached |

## The landmarks, which are the part to read

A landmark is the only field in a skill that carries a word the application chose rather than one this package generated. The screens refuse anything shaped like an address, a key, a link or a card number, and they cannot tell a piece of chrome from somebody's name. That judgement is the reason this file is in front of you.

| landmark | first used at step | is this chrome? |
| --- | --- | --- |
| `Private channels` | 4 | reviewer decides |

## What was screened before this was opened

- Every method, role and landmark passed the shapes in `skill.py`; a role is one of the closed vocabulary the accessibility layer uses.
- The rendered text was scanned for addresses, telephone numbers, payment cards, links and key-shaped strings, and carried none.
- The route completed 3 distinct times before it was submitted.
- The application is not one whose contents the desktop service withholds.

## What the screens could not answer

Whether each landmark above is a fixed part of this application's interface or a word that was on the screen that day. Whether the route goes somewhere an agent following it should be going. Both are yes-or-no questions with the route in front of you, and neither is a question a pattern can be written for.
